apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: openrouter
spec:
  refreshInterval: {{ .Values.refreshInterval }}
  secretStoreRef:
    name: {{ .Values.secretStoreRef.name }}
    kind: {{ .Values.secretStoreRef.kind }}
  target:
    name: openrouter
    template:
      type: Opaque
      data:
        api_key: "{{ `{{ .api_key }}` }}"
  data:
    - secretKey: api_key
      remoteRef:
        key: {{ .Values.vaultPrefix }}/openrouter
        property: api_key
