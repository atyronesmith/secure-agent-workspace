# secure-agent-workspace

> This is a [Validated Pattern](https://validatedpatterns.io/) generated by [QuickPat](https://github.com/atyronesmith/quickpat) from the [secure-agent-workspace](https://github.com/validatedpatterns-sandbox/secure-agent-workspace) AI Quickstart.

## About the Quickstart

Isolated, per-user AI agent sandboxes on OpenShift Virtualization. Each user gets a dedicated KubeVirt VM running NVIDIA OpenShell with OIDC authentication (RHBK) and Vault-managed inference provider keys.

For full details, see the [original quickstart README](https://github.com/validatedpatterns-sandbox/secure-agent-workspace#readme).

## What This Pattern Provides

- GitOps deployment via ArgoCD
- Operator lifecycle management (OpenShift Virtualization, Red Hat Build of Keycloak)
- HashiCorp Vault secret management
- Multi-cloud support (AWS, Azure, GCP, IBM Cloud)

## Deploy

```bash
# Clone and deploy
oc login <cluster>
./pattern.sh make install
```

The pattern must be on a git remote accessible by the cluster. If starting from a local copy, push it to a git repo first.

## Directory Structure

```
values-global.yaml          # Global config, multisource settings
values-prod.yaml            # Cluster group: namespaces, operators, apps
values-secret.yaml.template # Vault secrets template
Makefile                    # Build targets
pattern.sh                  # VP utility container runner
charts/                     # Helm chart(s)
overrides/                  # Platform-specific value overrides
docs/                       # Quickstart analysis report
```

## Documentation

See [`docs/quickstart-analysis.md`](docs/quickstart-analysis.md) for detailed analysis of the original quickstart.
