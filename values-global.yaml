# Generated by quickpat compose — do not edit
---
global:
  pattern: secure-agent-workspace
  singleArgoCD: true
  secretLoader:
    disabled: false
  # WORKAROUND (not regenerated by quickpat compose -- see AGENTS.md fix-later note):
  # quickpat's generator emits no sshPublicKey placeholder here at all, so
  # generate-keys.sh's sed patch silently no-ops. Re-apply after every compose.
  sshPublicKey: "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAICEZbWUA9A2is6gRxHoNwHN6NRvpN+AnO/RLdXDgPmY1 openshell-saw"
main:
  clusterGroupName: prod
  multiSourceConfig:
    enabled: true
    clusterGroupChartVersion: 0.9.*
